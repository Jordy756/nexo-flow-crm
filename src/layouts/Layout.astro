---
import "@styles/global.css";
import { Font } from "astro:assets";

interface Props {
  title: string;
  description?: string;
}

const { title, description = "CRM inteligente para conversaciones y seguimiento" } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="description" content={description} />

    <title>{title}</title>
    <Font cssVariable="--font-dm-sans" preload />

    <script src="../scripts/config/lenis.ts"></script>
    <script src="./prueba.js"></script>
  </head>
  <body class="bg-neutral-950 text-neutral-50 text-pretty">
    <div id="root" class="relative flex flex-col gap-12 min-h-dvh overflow-clip items-center">
      <div class="fixed inset-0 -z-10 before:absolute before:inset-0 before:bg-black/50">
        <canvas id="canvas" class="w-full h-full"></canvas>
      </div>
      <slot />
    </div>
  </body>
</html>
