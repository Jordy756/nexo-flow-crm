---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<video class:list={["fixed inset-0 object-cover -z-10", className]} muted autoplay loop>
  <source src="/videos/hero.mp4" />
</video>
