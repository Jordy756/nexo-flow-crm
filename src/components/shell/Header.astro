---
import Navbar from '@components/ui/Navbar.astro';

const pathname = Astro.url.pathname;
---

<header
  class:list={[
    'sticky top-0 left-0 z-40 flex w-full justify-center',
    pathname === '/' && 'animate-slide-in-top animate-delay-3200',
  ]}
>
  <Navbar />
</header>
